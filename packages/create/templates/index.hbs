import { bootstrap, runMigrations } from '@deenruv/core';
import { config } from './deenruv-config';

runMigrations(config)
    .then(() => bootstrap(config))
    .catch(err => {
        console.log(err);
    });
