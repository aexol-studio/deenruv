import { Input } from '@deenruv/react-ui-devkit';

export const Search = () => {
  return (
    <div className="w-[400px]">
      <Input className="w-full" placeholder={`Search in promotions by name`} />
    </div>
  );
};
